# V3.1.1 — Katalog sırası ve öne çıkan ürün düzeltmesi

## Katalog sırası
- Yeni ürün artık son katalog sırasına `+1` eklenerek oluşturulur. Örnek: 14 → 15 → 16.
- Ürün kopyalama da `+1` kullanır.
- Sürükle-bırak sonrası sıra `1, 2, 3...` şeklinde yeniden numaralanır; `10, 20, 30...` sistemi kaldırıldı.
- Kalıcı BGStudio3D veritabanı şeması V3'e yükseltildi. Mevcut `1..14, 24, 34` gibi değerler ilk V3.1.1 açılışında mevcut görsel sıralama korunarak `1..16` biçiminde otomatik normalize edilir.
- Mevcut ürünler, ürün içerikleri, renkler, görseller ve diğer ayarlar silinmez.

## Öne çıkan ürünler
- Ana sayfadaki 8 ürün sınırı kaldırıldı.
- `Ana sayfada öne çıkar` işaretli tüm aktif ürünler ana sayfada gösterilir.
- Yayın kontrolündeki `8'den fazla öne çıkan` uyarısı kaldırıldı; öne çıkan sayısı yalnız bilgi olarak gösterilir.

## Dağıtım
- Repo-safe patch'tir. `.git` içermez.
